using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMove : MonoBehaviour
{
    //private PlayerInput _input;
    //private Vector3 Movevec;
    //private Animator _animator;
    //public float Fspeed;
    //public LayerMask WallLayer;
    //void Move()
    //{
    //    Movevec = new Vector3(_input.X, 0f, _input.Z);
    //    Vector3 Pos = transform.position + new Vector3(0, 05f, 0);
    //    Vector3 PosEnd = transform.position + transform.forward * 0.5f;
    //    if(Physics.Linecast(Pos,PosEnd, WallLayer))
    //    {

    //    }
    //    else
    //    {
    //        transform.position += Movevec.normalized * Fspeed * Time.deltaTime;
    //    }
    //    if(Movevec != Vector3.zero)
    //    {
    //        transform.rotation = Quaternion.RotateTowards(transform.rotation, Quaternion.LookRotation(Movevec.normalized), 5);
    //    }

    //}
}

